To get started:

1. Create .env (look at .env.example for the keys)

If you want to run the cli: `uv run agent.py`

If you want to run the frontend:

1. `uv run api.py`
2. `cd ui` and then `npm run dev`